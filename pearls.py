def checkio(pearls: str,
            steps: int) -> float:

    switch = lambda c, s: s.replace( 
    check = lambda i, p: 
    first = (1, pearls)
        
    
    

print(checkio('bbw', 3))# == 0.48
